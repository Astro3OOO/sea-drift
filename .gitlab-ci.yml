stages:
  - build
  - test

build-image:
  stage: build
  image: docker:latest
  tags:
    - shell
  script:
    - docker build -t opendrift-container .
    - pytest tests

test-container:
  stage: test
  image: docker:latest
  tags:
    - shell
  script:
    - docker run opendrift-container python main.py input_test.json
